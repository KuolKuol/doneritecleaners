<div class="service">
    <img src="{{$image}}" alt="" class="service__image">
    <section class="service__section">
        <h1 class="service__title">{{$title}}</h1>
        <p class="service__summary">{{$summary}}</p>
        <a href="{{ url($link) }}" class="service__link">{{$linkTitle}}</a>
    </section>
</div>